{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  

  "scripts": {
    "test:e2en": "cypress run --e2e --headed --browser=chrome --config videoRecording=false",
    "test:e2end": "cypress run --e2e --headed --browser=chrome --config videoRecording=false"
  },

  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0"
  }
}
